[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "claude-kernel"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["ipykernel>=6.0"]

[tool.setuptools.packages.find]
include = ["claude_kernel*"]
